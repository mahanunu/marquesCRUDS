{% extends 'base.html.twig' %}

{% block title %}Marques index{% endblock %}

{% block body %}
    <h1>Marques index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>CreationDate</th>
                <th>Logo</th>
                <th>actions</th>
            </tr>git 
        </thead>
        <tbody>
        {% for marque in marques %}
            <tr>
                <td>{{ marque.id }}</td>
                <td>{{ marque.name }}</td>
                <td>{{ marque.creationDate ? marque.creationDate|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ marque.logo }}</td>
                <td>
                    <a href="{{ path('app_marques_show', {'id': marque.id}) }}">show</a>
                    <a href="{{ path('app_marques_edit', {'id': marque.id}) }}">edit</a>
                </td>
            </tr>

        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_marques_new') }}">Create new</a>
{% endblock %}
