{% extends 'base.html.twig' %}

{% block title %}Marques{% endblock %}

{% block body %}
    <h1>Marques</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marque.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ marque.name }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ marque.creationDate ? marque.creationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ marque.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_marques_index') }}">back to list</a>

    <a href="{{ path('app_marques_edit', {'id': marque.id}) }}">edit</a>

    {{ include('marques/_delete_form.html.twig') }}
{% endblock %}
