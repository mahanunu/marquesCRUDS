{% extends 'base.html.twig' %}

{% block title %}Modeles index{% endblock %}

{% block body %}
    <h1>Modeles</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Price</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for modele in modeles %}
            <tr>
                <td>{{ modele.id }}</td>
                <td>{{ modele.name }}</td>
                <td>{{ modele.price }}</td>
                <td>
                    <a href="{{ path('app_modeles_show', {'id': modele.id}) }}">show</a>
                    <a href="{{ path('app_modeles_edit', {'id': modele.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_modeles_new') }}">Create new</a>
{% endblock %}
